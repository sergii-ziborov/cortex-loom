# Cortex Loom

Keep the protocol-independent graph, routing, and skill compiler independent of MCP and UI transports. Local models may draft or compress evidence, but high-risk or unverified work must fall back to the upstream coding agent. Never auto-apply Weavatrix Refactor changes; preserve its preview and confirmation boundary.

Keep source files below 500 lines. Run `cargo fmt --all -- --check`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `npm.cmd --prefix ui run build` before publishing.

