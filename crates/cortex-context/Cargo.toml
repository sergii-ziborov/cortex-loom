[package]
name = "cortex-context"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
# The repository field is intentionally absent: the workspace URL points at a
# private repository and would 404 for every crates.io visitor. Add it back
# once a public repository exists.
license = "MIT OR Apache-2.0"
description = "Deterministic, budget-bounded evidence selection and retrieval ranking for LLM context packets."
documentation = "https://docs.rs/cortex-context"
readme = "README.md"
keywords = ["llm", "context", "retrieval", "bm25", "ranking"]
categories = ["algorithms", "text-processing"]
publish = true

[dependencies]
serde.workspace = true

[lints]
workspace = true
