[package]
name = "cortex-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
# The repository field is intentionally absent: the workspace URL points at a
# private repository and would 404 for every crates.io visitor. Add it back
# once a public repository exists.
license = "MIT OR Apache-2.0"
description = "Transport-independent typed process-graph schema with structural validation invariants."
documentation = "https://docs.rs/cortex-domain"
readme = "README.md"
keywords = ["graph", "workflow", "schema", "validation", "agents"]
categories = ["data-structures", "development-tools"]
publish = true

[dependencies]
blazingly-json.workspace = true
serde.workspace = true

[lints]
workspace = true
