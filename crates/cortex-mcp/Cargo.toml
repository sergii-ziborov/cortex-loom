[package]
name = "cortex-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[dependencies]
cortex-adapters = { path = "../cortex-adapters" }
cortex-context = { path = "../cortex-context" }
cortex-domain = { path = "../cortex-domain" }
cortex-ollama = { path = "../cortex-ollama" }
cortex-run = { path = "../cortex-run" }
cortex-router = { path = "../cortex-router" }
cortex-shadow = { path = "../cortex-shadow" }
cortex-skills = { path = "../cortex-skills" }
cortex-store = { path = "../cortex-store" }
cortex-weavatrix = { path = "../cortex-weavatrix" }
mcport.workspace = true
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true
