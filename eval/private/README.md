# Private eval

Reserved hidden suite. Do not use these cases when writing heuristics,
prompts, or training data. The public fixtures are already the development
gate; this directory is the hold-out that development must not touch.

A release claim that cites only `crates/cortex-eval/fixtures/` is a
development number, not a hidden-suite number.
