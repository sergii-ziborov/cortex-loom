# Training-only Python. Not a product runtime: none of this is in Cargo, in the
# OVMS bundle, or on any Cortex request path. See README.md next to this file.
#
# Windows PyPI torch wheels are CPU-only and this box has no CUDA device, so
# 4-bit bitsandbytes is unavailable: this is LoRA in fp32 on CPU, not QLoRA.
# Pinned to what was resolved and used on 2026-08-14 under Python 3.12.13.
torch==2.13.0
transformers==5.15.0
peft==0.20.0
accelerate==1.14.0
safetensors==0.8.0
huggingface-hub==1.27.0
numpy==2.5.2
# Needed only by export_ollama.py. llama.cpp's converter tries the SentencePiece
# vocab path first and falls back to BPE on FileNotFoundError; with the module
# absent it raises ModuleNotFoundError instead, the fallback never runs, and
# Qwen3 — which is BPE — fails to convert with a misleading error.
sentencepiece==0.2.2
